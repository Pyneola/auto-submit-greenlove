# LMS Auto Submit Bot (Playwright)

สคริปต์นี้เป็นระบบ **อัตโนมัติสำหรับส่งงานบน LMS PSU** โดยใช้ Python + Playwright พร้อมระบบแจ้งเตือนผ่าน Discord Webhook เหมาะสำหรับงานที่ต้องส่งซ้ำทุกวัน เช่น Challenge / Bonus Activity

> ⚠️ ใช้เพื่อการศึกษาและอำนวยความสะดวกส่วนตัวเท่านั้น ผู้ใช้ต้องรับผิดชอบต่อการใช้งานเอง

---

## ความสามารถหลัก (Features)

- Login LMS อัตโนมัติ
- ค้นหาลิงก์ส่งงานตามวันที่ปัจจุบัน (ภาษาไทย)
- อัปโหลดไฟล์รูปภาพอัตโนมัติ
- ใส่คอมเมนต์อัตโนมัติ (นับจำนวนครั้งจากวันเริ่มต้น)
- ตรวจสอบว่ามีการส่งงานไปแล้วหรือยัง (Add / Edit submission)
- แจ้งเตือนผลลัพธ์ผ่าน Discord (สำเร็จ / ล้มเหลว)
- มีระบบ Log เก็บประวัติการทำงาน

---

## โครงสร้างไฟล์ (Project Structure)

```
project/
├── main.py          # สคริปต์หลัก
├── config.py        # ไฟล์ตั้งค่า (username, password, webhook ฯลฯ)
├── greenlove.jpg    # รูปภาพที่ใช้ส่งงาน
├── app.log          # Log การทำงาน (สร้างอัตโนมัติ)
├── README.md        # เอกสารอธิบายโปรเจกต์ (ไฟล์นี้)
```

---

## ความต้องการของระบบ (Requirements)

- Python **3.9+**
- Google Chrome / Chromium
- อินเทอร์เน็ต

### Python Libraries ที่ใช้

- playwright
- requests

---

## การติดตั้ง (Installation)

### 1️ สร้าง Virtual Environment (แนะนำ)

```bash
python -m venv venv
```

**Windows**

```bash
venv\Scripts\activate
```

**macOS / Linux**

```bash
source venv/bin/activate
```

---

### 2️ ติดตั้ง Library ที่จำเป็น

```bash
pip install -r requirements.txt
```

ติดตั้ง Browser สำหรับ Playwright

```bash
playwright install
```

---

## การตั้งค่า (Configuration)

แก้ไขไฟล์ `config.py`

```python
class AppConfig:
    USERNAME = "รหัสนักศึกษา"
    PASSWORD = "รหัสผ่าน LMS"

    DISCORD_WEBHOOK = "https://discord.com/api/webhooks/..."

    COURSE_URL = "https://lms.psu.ac.th/course/view.php?id=XXXX"

    IMAGE_FILENAME = "greenlove.jpg"

    START_DATE = datetime.date(2026, 2, 6)

    LOG_FILE = "app.log"
```

## การตั้งค่า Discord Webhook

Discord Webhook ใช้สำหรับแจ้งเตือนสถานะการทำงานของสคริปต์แบบเรียลไทม์ เช่น ส่งงานสำเร็จ, วันนี้ส่งไปแล้ว, หรือเกิด Error

---

### ขั้นตอนการสร้าง Discord Webhook

1. เปิด Discord และเข้า **Server** ที่ต้องการรับแจ้งเตือน
2. คลิกขวาที่ **Channel** → เลือก **Edit Channel**
3. ไปที่เมนู **Integrations**
4. เลือก **Webhooks** → กด **New Webhook**
5. ตั้งชื่อ เช่น `Auto LMS Bot`
6. เลือก Channel ที่ต้องการให้แจ้งเตือน
7. กด **Copy Webhook URL**

---

### การนำ Webhook ไปใช้งาน

นำ URL ที่ได้ ไปใส่ในไฟล์ `config.py`

```python
class AppConfig:
    DISCORD_WEBHOOK = "https://discord.com/api/webhooks/XXXXXXXX"
```

หาก **ไม่ต้องการใช้ Discord แจ้งเตือน**

```python
DISCORD_WEBHOOK = None
# หรือ
DISCORD_WEBHOOK = ""
```

ระบบจะข้ามขั้นตอนการแจ้งเตือนอัตโนมัติ

---

### รูปแบบข้อความแจ้งเตือน

Webhook จะส่งในรูปแบบ **Embed Message** โดยมีสีแยกตามสถานะ

- 🟢 สีเขียว → ส่งงานสำเร็จ / Skip อย่างปลอดภัย
- 🔴 สีแดง → เกิดข้อผิดพลาด

ข้อมูลที่ส่งไป เช่น

- วันที่
- หัวข้องาน
- คอมเมนต์
- ชื่อไฟล์
- ข้อความ Error (ถ้ามี)

---

## การแก้ไขข้อความ Comment

สคริปต์นี้จะใส่คอมเมนต์ให้อัตโนมัติหลังจากอัปโหลดไฟล์เรียบร้อยแล้ว โดยข้อความคอมเมนต์สามารถแก้ไขได้ง่ายจากในโค้ด

---

### ตำแหน่งที่ใช้แก้ไข Comment

เปิดไฟล์ `main.py` แล้วมองหาส่วนนี้

```python
comment_text = f"ครั้งที่ {days_diff} ลดขยะพลาสติกโดยขวดแก้ว วันที่ {today_str}"
```

ผู้ใช้สามารถแก้ไขข้อความภายใน `comment_text` ได้ตามต้องการ เช่น

```python
comment_text = f"ครั้งที่ {days_diff} ส่งกิจกรรมรักษ์โลก วันที่ {today_str}"
```

หรือใส่ข้อความแบบคงที่ (ไม่อิงวันที่)

```python
comment_text = "ส่งกิจกรรมเรียบร้อยครับ"
```

---

### หลักการทำงานของ Comment

- ระบบจะพยายามกดปุ่ม **Comments** ก่อน
- หากยังไม่เห็นช่องพิมพ์ จะกด **Add a comment...** อัตโนมัติ
- ใส่ข้อความจากตัวแปร `comment_text`
- กด **Save comment**

> หากใส่คอมเมนต์ไม่สำเร็จ ระบบจะ **ไม่ยกเลิกการส่งไฟล์** และจะทำงานต่อทันที

---

### ข้อควรระวัง

- หากอาจารย์ปิดการคอมเมนต์ ระบบจะไม่สามารถใส่ได้
- โครงสร้างหน้า LMS อาจเปลี่ยน ทำให้ selector ไม่ตรง
- ในกรณี error จะมี log แจ้งไว้ใน `app.log`

### คำแนะนำด้านความปลอดภัย

- **ไม่ควร commit `config.py` ขึ้น GitHub**
- แนะนำให้ใส่ใน `.gitignore`
- ใช้ Webhook เฉพาะส่วนตัวเท่านั้น

---

## วิธีใช้งาน (Usage)

รันสคริปต์ด้วยคำสั่ง

```bash
python main.py
```

ระบบจะทำงานตามลำดับ:

1. ตรวจสอบไฟล์และ config
2. Login LMS
3. เข้า Course
4. ค้นหาลิงก์ส่งงานของวันนี้
5. ตรวจสอบว่าส่งไปแล้วหรือยัง
6. อัปโหลดไฟล์ + Save
7. ใส่คอมเมนต์
8. แจ้งผลผ่าน Discord

---

## รูปแบบการแจ้งเตือน Discord

### สำเร็จ

- วันที่ส่งงาน
- ชื่องาน
- คอมเมนต์
- ชื่อไฟล์

### ล้มเหลว

- Login ไม่ผ่าน
- ไม่พบลิงก์งาน
- ไม่พบปุ่มส่งงาน
- Error ระหว่างทำงาน

---

## Logic สำคัญที่ควรรู้

### การป้องกันการส่งซ้ำ

- หากพบปุ่ม **Edit submission**
- ระบบจะ **หยุดทำงานทันที**
- แจ้ง Discord ว่า "วันนี้ส่งไปแล้ว"

### การนับจำนวนครั้ง

```python
days_diff = (วันนี้ - START_DATE) + 1
```

ใช้สำหรับสร้างข้อความคอมเมนต์อัตโนมัติ

---

## Log File (`app.log`)

ระบบจะบันทึกทุกขั้นตอน เช่น

```
[2026-02-06 09:00:01] Login สำเร็จ
[2026-02-06 09:00:10] อัปโหลดไฟล์สำเร็จ
```

ใช้สำหรับ Debug เมื่อเกิดปัญหา

---

## ปัญหาที่พบบ่อย (Troubleshooting)

### ❌ Login ไม่ผ่าน

- ตรวจสอบ USERNAME / PASSWORD
- LMS อาจล่มหรือช้า

### ❌ ไม่พบลิงก์งาน

- อาจารย์ยังไม่โพสต์
- ชื่อลิงก์เปลี่ยน

### ❌ อัปโหลดไฟล์ไม่ได้

- ชื่อไฟล์ผิด
- ไฟล์ไม่อยู่ในโฟลเดอร์เดียวกับสคริปต์

---

## การตั้งค่า Task Scheduler (Windows Auto Run)

เพื่อให้สคริปต์ทำงานอัตโนมัติทุกวันตามเวลาที่กำหนด (เช่น 08:00 น.) โดยไม่ต้องกดรันเอง สามารถตั้งค่าผ่าน Windows Task Scheduler ได้ดังนี้

#### 1. สร้าง Task ใหม่

    เปิดโปรแกรม Task Scheduler

    คลิกขวาที่โฟลเดอร์ Task Scheduler Library

    เลือกเมนู Create Task...
<img width="361" height="307" alt="Screenshot 2026-02-07 080354" src="https://github.com/user-attachments/assets/1ff8c111-d057-46e7-8c2d-84feb19c269a" />


#### 2. ตั้งค่าทั่วไป (General)

    Name: ตั้งชื่อ Task (เช่น auto submit)

<img width="488" height="147" alt="Screenshot 2026-02-07 080412" src="https://github.com/user-attachments/assets/133aab27-4252-45ed-a9df-4afbc0c263a0" />

    Security options:

        เลือก 🔘 Run whether user is logged on or not (เพื่อให้โปรแกรมทำงานเบื้องหลังได้ แม้จะ Lock หน้าจออยู่)

        Configure for: เลือก Windows 10
        
<img width="789" height="165" alt="Screenshot 2026-02-07 080448" src="https://github.com/user-attachments/assets/a80c780f-e478-4975-95b7-9a3c442cf8c3" />


#### 3. ตั้งเวลาทำงาน (Triggers)

    ไปที่แท็บ Triggers กดปุ่ม New... เพื่อสร้างเงื่อนไขเวลา

    แบบรายวัน (Daily):
        Begin the task: On a schedule

    Settings:
        🔘 Daily

    Start:
        ตั้งเวลาที่ต้องการให้ส่งงาน (เช่น 8:00:00)

    Recur every:
        1 days

<img width="733" height="633" alt="Screenshot 2026-02-07 080509" src="https://github.com/user-attachments/assets/5bd8e806-0874-4966-abfd-e6067c19f7e9" />


    (เผื่อระบบรีสตาร์ท) At startup:
        เพิ่ม Trigger อีกอัน เลือก Begin the task: At startup

<img width="730" height="648" alt="Screenshot 2026-02-07 080521" src="https://github.com/user-attachments/assets/96a96bd6-cc46-4c76-b790-4adffdda5ce5" />

<img width="774" height="342" alt="Screenshot 2026-02-07 080531" src="https://github.com/user-attachments/assets/9da569bc-3b6b-4a6f-8bbb-4a18a89c4881" />


เพื่อให้ระบบพยายามรันทันทีเมื่อเปิดเครื่องใหม่

#### 4. ตั้งค่าคำสั่งรัน (Actions)

    ไปที่แท็บ Actions กดปุ่ม New...

    Action: Start a program
        Program/script: เลือกไฟล์ main.py ของคุณ
        ตัวอย่าง: D:\Greenlove\main.py

    Start in (optional): สำคัญมาก ต้องใส่ Path ของโฟลเดอร์ที่เก็บโปรเจกต์
        ตัวอย่าง: D:\Greenlove

    (หากไม่ระบุช่องนี้ สคริปต์จะหาไฟล์รูปภาพหรือ config ไม่เจอ)

<img width="566" height="361" alt="Screenshot 2026-02-07 080557" src="https://github.com/user-attachments/assets/14d285e2-8096-4a55-a9dc-0028b3d17b32" />

#### 5. เงื่อนไขเพิ่มเติม (Conditions & Settings)

    Tab Conditions:
        ❌ ติ๊กออก Start the task only if the computer is on AC power (เพื่อให้ทำงานได้แม้ใช้แบตเตอรี่โน้ตบุ๊ก)

<img width="462" height="207" alt="Screenshot 2026-02-07 080609" src="https://github.com/user-attachments/assets/771a2d28-3207-4580-bfe2-c0bcf30c93a5" />

    Tab Settings:
        ✅ ติ๊ก Allow task to be run on demand (เพื่อให้กด Run ทดสอบเองได้)
        ✅ ติ๊ก If the task fails, restart every: เลือก 5 minutes (ช่วยให้ระบบลองใหม่หากเน็ตหลุดหรือ Error)
        ✅ ติ๊ก Stop the task if it runs longer than: 3 days (ป้องกัน process ค้าง)
    
<img width="677" height="356" alt="Screenshot 2026-02-07 080640" src="https://github.com/user-attachments/assets/7fabdc27-6dd7-47bf-b0f6-b7ec3e78068b" />

## License

ใช้เพื่อการศึกษา (Educational Purpose)

ผู้ใช้ต้องรับผิดชอบต่อการใช้งานและผลที่เกิดขึ้นเอง

---
